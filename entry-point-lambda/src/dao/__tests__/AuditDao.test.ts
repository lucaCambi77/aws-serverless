
jest.mock('aws-sdk/clients/dynamodb');

describe('Dao', () => {

    beforeEach(() => {
        jest.clearAllMocks();
    });

    describe('put item', () => {

        test('dao put new item with expected data and call promise', async () => {
            //
        });
    });
});
