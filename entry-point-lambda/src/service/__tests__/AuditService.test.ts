jest.mock('aws-sdk/clients/dynamodb');

describe('Service Unit Test', () => {

    beforeEach(() => {
        jest.clearAllMocks();
    });

    describe('update', () => {

        test('service calls dao', async () => {
            //
        });
    });
});
